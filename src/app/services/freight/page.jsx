// Example for src/app/services/freight/page.js

// You must use "use client" if this component uses hooks or client-side features,
// but for a basic static page, you can often omit it.

export default function FreightPage() {
  return (
    <div style={{ padding: '20px' }}>
      <h1>Freight Services</h1>
      <p>This page is currently under construction. Please check back later!</p>
    </div>
  );
}